services:
  game-qna-api:
    build:
      context: .
    env_file:
      - .env
    environment:
      PORT: 3000
      AGENT_PUBLIC_URL: "http://game-qa-agent:3000"
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
